name: "warmup_dbt"
version: "1.0.0"
config-version: 2
profile: "default"
model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  warmup_dbt:
    tap_csv:
      +materialized: view
